# 교육부 기본 어휘 3,000 단어장·문제집 앱 — Claude Code 작업 지시

## 0. 배경
- 데이터 출처: 교육부 고시 제2022-33호 [별책 14] 영어과 교육과정 부록 (공공 고시 자료, 저작권 제한 없음)
- 앱 성격: 초·중·고 학생용 영단어 암기 + 문제집. PWA(모바일 설치형), 무료. "교육부 고시 기본 어휘 3,000 기반"으로 출처 표기하되 교육부 공식 앱처럼 보이게 하지 말 것.
- 이 폴더의 `data/` 파일이 원본 데이터. 파일 구조는 아래 참고.

## 1. 입력 데이터
| 파일 | 내용 |
|---|---|
| `data/words.csv` | 3,001단어. 컬럼: `word, stars, level, alt_spelling, derived` — stars `*`=초등(801) `**`=중고공통(1,200) 없음=선택(1,000). `alt_spelling`은 영미 철자 변이(colour 등), `derived`는 파생어(세미콜론 구분) |
| `data/functions.csv` | 의사소통 기능 예시문 545개. 컬럼: `domain, category, function, sentence`. `...`, `X/Y` 같은 자리표시자 포함 |
| `data/grammar.csv` | 언어 형식 예문 339개. 컬럼: `category(문법 범주 39종), sentence, level(초/중/고)` |
| `data/curriculum_2022.json` | 위 세 파일을 하나로 묶은 JSON |

## 2. 작업 단계 (순서대로, 각 단계 끝나면 결과 요약 후 확인받고 다음으로)

### 단계 A — 데이터 보강 (스크립트: `scripts/enrich.py`)
`words.csv` → `data/words_enriched.json` 생성. 단어별 필드:
```
{ id, word, level, stars, alt_spelling, derived[],
  ipa,                // 미국식 IPA. 우선 CMUdict(pip: cmudict) → IPA 변환, 실패 시 Free Dictionary API(api.dictionaryapi.dev) 조회, 그래도 없으면 빈 문자열로 두고 missing_ipa.txt에 기록
  pos[],              // 품사 (여러 개 가능)
  meanings[],         // 한글 뜻. 품사별로 1~3개, 학생용 간결한 표현
  examples[ {en, ko} ], // 예문 2개. level이 초등이면 6단어 이내 쉬운 문장, 중고공통은 12단어 이내, 선택은 15단어 이내. 예문 안에서 해당 단어(굴절형 포함)를 반드시 사용
  theme,              // 주제 태그 1개 (가족/학교/감정/음식/자연/시간/장소/신체/직업/과학/사회/추상 등 약 20개 고정 목록에서 선택)
  day                 // 아래 단계 B에서 배정
}
```
- 뜻·품사·예문·theme는 Anthropic API(claude-sonnet 계열)로 생성. 50단어씩 배치, JSON만 반환하도록 시스템 프롬프트 지정, 재시도·캐시(`cache/`) 구현해서 중단 후 재개 가능하게.
- `functions.csv`에 해당 단어가 이미 쓰인 문장이 있으면 그 문장을 첫 번째 예문으로 우선 채택(자리표시자 `...`가 포함된 문장은 제외), 부족분만 생성.
- 생성 후 검증 스크립트: 단어가 예문에 실제 포함되는지, JSON 필드 누락 없는지, 뜻이 비었는지 체크 → `validation_report.md`.

### 단계 B — Day 배정
- 트랙: 초등(Day 1–16) → 중고공통(Day 17–40) → 선택(Day 41–60). 하루 50단어.
- 각 등급 안에서 알파벳순 그대로 쓰지 말고 theme·품사를 섞어 하루 안에 다양한 첫 글자가 오도록 배분. 파생어가 있는 단어는 균등 분산.
- 문법(`grammar.csv`)은 별도 메뉴로 level(초/중/고)별 분류. 단어 Day와 무관.

### 단계 C — 앱 구현
- 스택: Vite + React + TypeScript, PWA(vite-plugin-pwa), 상태·진도는 IndexedDB(idb 라이브러리). 서버 없음, 완전 오프라인 동작.
- 음성: Web Speech API(`speechSynthesis`, en-US 음성) 기본. 음성 재생 유틸을 한 곳에 두어 나중에 mp3로 교체 가능하게.
- 화면:
  1. **홈** — 진단테스트 진입 / 트랙(초·중고·심화) 선택 / 오늘 학습 Day 카드 + 원형 진행률 + 오늘 복습 대기 수
  2. **진단테스트** — 각 등급에서 10개씩 30문항, 정답률로 시작 Day 추천
  3. **암기 카드**
     - 앞면: 단어, IPA, 🔊(단어). 카드 열릴 때 자동 재생 옵션
     - 뒷면: 품사별 뜻, 예문 2개 + 🔊(예문), 파생어
     - 하단 버튼: 모름 / 헷갈림 / 앎
     - 모드: ①학습(뒷면 자동 공개) ②셀프 테스트(앞면만, 탭해서 뒤집기) ③오답 노트(모름·헷갈림만, 3회 연속 '앎'이면 졸업) ④듣기 모드(화면 어둡게, 단어→뜻→예문 음성 순차 재생, 간격·반복 설정)
     - 토글: 영→한 / 한→영, 카드 셔플
  4. **문제집** — 유형: 단어→뜻 4지선다, 뜻→단어 4지선다, 철자 입력, 듣고 쓰기(음성 재생 후 입력), 예문 빈칸, 파생어 문제(`derived` 활용). 오답 선택지는 같은 level·같은 품사에서 뽑을 것.
  5. **문법** — grammar.csv를 범주별로 보여주고 level 필터. 예문 🔊. 문제: 예문 빈칸/어순 배열.
  6. **복습** — 간격 반복: 모름→1일 / 헷갈림→3일 / 앎→7일→30일. 매일 due 카드 자동 큐. 7일마다 누적 테스트 100문항, 80% 미만이면 재시험 안내.
  7. **통계** — Day별 정답률, 상태(미학습/학습중/헷갈림/완료) 분포, 연속 학습일
- 단어 상태 모델: `unseen → learning → confused → mastered`.
- 디자인: 모바일 우선, 큰 터치 영역, 다크모드 지원. 한 손 조작 가능하게 판정 버튼은 하단 고정.
- 데이터는 빌드 시 `words_enriched.json`을 청크(Day별)로 나눠 로드해 초기 로딩 가볍게.

### 단계 D — 마무리
- `README.md`: 데이터 출처, 재생성 방법(enrich.py 사용법), 빌드·배포(Vercel 또는 Firebase Hosting) 절차
- 설정 화면에 "진도 초기화", "데이터 내보내기/가져오기(JSON)" 포함
- 앱 하단 소개에 출처 문구: "단어 목록: 교육부 고시 제2022-33호 영어과 교육과정 기본 어휘. 뜻·예문·음성은 본 앱에서 자체 제작."

## 3. 규칙
- 단계별로 먼저 계획을 보여주고 승인 후 진행.
- API 키는 `.env`에서 읽고 절대 코드에 넣지 않기.
- 3,001단어라 문서 기준(3,000)과 1개 차이 — 단계 A 검증 시 중복·이상 항목 있는지 확인하고 보고.
- 코드 주석·커밋 메시지는 한국어.
